#include "Behaviors.h"

namespace UCG {
  
	TileSet Behaviors::Target(GameScene* context, const Monster& subject, ObjectSelectFlags flags, uint32_t numTargets, const TileSet& excluded, const TileSet& included) {
  
		return TileSet();
	}

}
